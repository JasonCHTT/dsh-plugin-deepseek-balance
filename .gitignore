# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local runtime artifacts (the plugin writes its baseline to the dsh home,
# outside the repo — kept here in case it ever runs inside the repo)
deepseek-balance.json
